{
  "name": "copy-github-url",
  "displayName": "copy-github-url",
  "description": "Copy Github URL",
  "version": "0.0.4",
  "publisher": "t-mrt",
  "license": "MIT",
  "engines": {
    "vscode": "^1.11.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.copyGithubURL"
  ],
  "main": "./out/src/extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.copyGithubURL",
        "title": "Copy Github URL"
      }
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/t-mrt/copy-github-url.git"
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.32",
    "@types/node": "^6.0.40",
    "mocha": "^2.3.3",
    "tslint": "^5.1.0",
    "typescript": "2.3.2",
    "vscode": "^1.0.0"
  },
  "dependencies": {
    "copy-paste": "^1.3.0"
  }
}
